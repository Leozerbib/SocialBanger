import { SubscriptionPlus } from './subscription-plus.model';

describe('SubscriptionPlus', () => {
  it('should create an instance', () => {
    expect(new SubscriptionPlus()).toBeTruthy();
  });
});
